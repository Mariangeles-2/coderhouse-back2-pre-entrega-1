# 🛍️ ECOMMERCE BACKEND - CONFIGURACIÓN DE ENTORNO
# Copia este archivo como .env y completa los valores

# ====================================
# 🚀 CONFIGURACIÓN DEL SERVIDOR
# ====================================
PORT=8080
NODE_ENV=development

# ====================================
# 🗄️ BASE DE DATOS MONGODB
# ====================================
# URL de conexión a MongoDB (local o MongoDB Atlas)
MONGODB_URI=mongodb://localhost:27017/ecommerce
# Opcional: para MongoDB Atlas
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/ecommerce

# ====================================
# 🔐 SECRETOS JWT
# ====================================
# Secreto para access tokens (cambiar por uno seguro de al menos 32 caracteres)
JWT_ACCESS_SECRET=tu-super-secreto-para-access-tokens-muy-largo-y-seguro

# Secreto para refresh tokens (diferente al access secret)
JWT_REFRESH_SECRET=tu-super-secreto-para-refresh-tokens-muy-largo-y-seguro

# Duración de los tokens
JWT_ACCESS_EXPIRY=15m
JWT_REFRESH_EXPIRY=7d

# ====================================
# 🔑 CONFIGURACIÓN DE SESIONES
# ====================================
# Secreto para las sesiones (cambiar por uno seguro)
SESSION_SECRET=tu-secreto-para-sesiones-muy-largo-y-seguro

# ====================================
# 📧 CONFIGURACIÓN DE EMAIL (SMTP)
# ====================================
# Configuración para Gmail (o tu proveedor SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=tu-email@gmail.com
SMTP_PASSWORD=tu-password-de-aplicacion

# ====================================
# 🌐 URLs Y FRONTEND
# ====================================
# URL del frontend para CORS y links de recuperación
FRONTEND_URL=http://localhost:3000

# Nombre de la aplicación (aparece en emails)
APP_NAME="Ecommerce Backend"

# ====================================
# 🛡️ CONFIGURACIÓN DE SEGURIDAD
# ====================================
# Entorno de producción (cambiar a 'production' en producción)
SECURE_COOKIES=false

# Dominios permitidos para CORS (separados por comas)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:8080

# ====================================
# 📊 LOGGING Y DEBUGGING
# ====================================
# Nivel de logging: error, warn, info, debug
LOG_LEVEL=info

# ====================================
# ⚡ RATE LIMITING
# ====================================
# Configuración de límites de requests (opcional, tienen valores por defecto)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
LOGIN_RATE_LIMIT_MAX=5
REGISTER_RATE_LIMIT_MAX=3

# ====================================
# 🔧 CONFIGURACIÓN ADICIONAL
# ====================================
# URL base de la API (opcional)
API_BASE_URL=http://localhost:8080

# Habilitar documentación Swagger en producción (true/false)
ENABLE_SWAGGER_IN_PRODUCTION=false

# ====================================
# 📝 INSTRUCCIONES
# ====================================
# 1. Copia este archivo como .env en la raíz del proyecto
# 2. Completa todos los valores marcados como "tu-..."
# 3. NUNCA subas el archivo .env a Git
# 4. Para Gmail, usa una "Contraseña de aplicación" en lugar de tu contraseña normal
# 5. En producción, usa secretos generados aleatoriamente de al menos 32 caracteres
# 6. Para generar secretos seguros, usa: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
